const widthPercent = Math.round((window.innerWidth / screen.width) * 100)
const heightPercent = Math.round((window.innerHeight / screen.height) * 100)
alert(`Den här vyn tar upp ${widthPercent}% av din skärmbredd, och ${heightPercent}% av din skärmhöjd`)